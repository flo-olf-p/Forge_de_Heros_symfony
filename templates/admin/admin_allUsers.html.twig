{% extends 'base.html.twig' %}

{% block body %}
    <ul>
        {% for user in users %}
        <li>{{ user.id }}  {{ user.username }}  {{ user.email }}</li>
        {% endfor %}
    </ul>
{% endblock %}

